package com.config;import org.aeonbits.owner.ConfigCache;public class ConfigurationManager {        //  получает или создает экземплляр из прочитанного интерфейса класса и получает все данные.        // gets or creates an instance from the read class Interface and will receive all data        public static Configuration config() {        return ConfigCache.getOrCreate ( Configuration.class );    }}